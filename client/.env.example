# Local dev: Vite proxies /api to the backend, so this can be the local server URL.
# In production, leave VITE_API_URL empty — same-origin requests via the Express static server.
VITE_API_URL=http://localhost:3001
